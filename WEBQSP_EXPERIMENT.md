# WebQSP 기반 외적 타당성 파일럿 실험

## 1. 실험 목적

본 실험의 목적은 WN18RR과 MAGIC에서 관찰한 가지치기 현상이 실제 KGQA 질의에서도 재현되는지 확인하기 위한 연결 검증이다. 특히 동일한 질의와 그래프 탐색 조건에서 Top-K, 전역 점수대, 상대 손실, 경계 인식 지연 가지치기(BADP)를 적용하고, 정답 도달률과 가지치기 후회, 보존 집합의 품질, 탐색 비용을 함께 측정한다.

이 파일럿은 ToG의 공개 WebQSP 질의 파일을 사용하지만, 그래프는 Wikidata를 사용한다. 따라서 ToG 또는 Paths-over-Graph가 보고한 Freebase 기반 WebQSP 성능을 재현하는 실험은 아니다. 본 실험의 역할은 **동일한 WebQSP 질문에서 가지치기 정책을 실제로 실행할 수 있는지 검증하고, 정책별 실패 양상을 확인하는 것**이다.

---

## 2. 실험 설정

### 2.1 데이터

- 데이터셋: WebQSP
- 질의 원본: ToG 공식 저장소의 `WebQSP.json`
- 파일럿 질의 수: 3
- 최대 추론 깊이: 2홉
- 시작 개체: `qid_topic_entity`
- 골드 정답: WebQSP `EntityName`

### 2.2 그래프

시작 QID에서 Wikidata의 entity-valued outgoing statement를 확장하였다. 공개 API 호출량을 제어하기 위해 각 노드에서 lexical overlap을 이용한 동일 후보 사전 제한을 모든 정책에 공통 적용하였으며, 최대 10개 edge를 의미 점수화 대상으로 사용하였다.

### 2.3 점수화

각 edge `e`에 대해 DeBERTa NLI 모델의 support 점수를 계산하였다.

\[
s(e,q)=P(\text{support}\mid e,q)
\]

경로 점수는 구성 edge의 평균으로 정의하였다.

\[
s(p,q)=\frac{1}{|p|}\sum_{e\in p}s(e,q)
\]

사용 모델은 다음과 같다.

`MoritzLaurer/DeBERTa-v3-base-mnli-fever-anli`

### 2.4 비교 정책

- Top-3
- Top-5
- 전역 점수대 `ε=.05`, `.10`
- 상대 손실 `.25`, `.50`
- BADP Top-3 `δ=.005`, `.010`
- BADP Top-5 `δ=.010`, `.050`

---

## 3. 평가 지표

### 3.1 정답 도달률

탐색 중 보존된 경로의 끝점 중 하나가 WebQSP 골드 정답과 일치하면 탐색 성공으로 판정하였다.

### 3.2 Hit@1

가장 높은 점수를 받은 경로의 끝점이 골드 정답이면 1로 계산하였다.

### 3.3 검색 F1

탐색 과정에서 보존한 endpoint와 골드 정답 집합 사이의 정밀도와 재현율을 이용한다.

### 3.4 정답 수준 가지치기 후회

특정 깊이에서 가지치기 전 후보 중 골드 정답 endpoint가 존재하였으나 가지치기 이후 모두 제거된 경우를 후회로 기록하였다.

### 3.5 비용

평균 활성 폭과 질의당 확장 후보 수를 기록하였다.

---

## 4. 실행 결과

실행 번호는 `32827441277`, artifact는 `9555602493`이다. 3개 질의에서 46회의 Wikidata API 호출과 228개의 고유 edge NLI 점수 계산이 수행되었다.

| 정책 | 정답 도달률 | Hit@1 | Macro 검색 F1 | 정답 재현율 | 가지치기 후회 ↓ | 평균 폭 | 확장 후보 |
|---|---:|---:|---:|---:|---:|---:|---:|
| **Top-3** | **66.67%** | 33.33% | **20.63%** | 66.67% | **0.00%** | **3.00** | 43.00 |
| Top-5 | 66.67% | 33.33% | 13.47% | 66.67% | 33.33% | 5.00 | 54.00 |
| 전역 `.05` | 33.33% | 33.33% | 11.11% | 33.33% | 33.33% | 2.67 | **28.33** |
| 전역 `.10` | 33.33% | 33.33% | 11.11% | 33.33% | 33.33% | 4.33 | 31.67 |
| 상대 손실 `.25` | 66.67% | 33.33% | 14.29% | 66.67% | 33.33% | 7.67 | 69.67 |
| 상대 손실 `.50` | 66.67% | 33.33% | 14.29% | 66.67% | 33.33% | 7.67 | 69.67 |
| BADP Top-3 `.005` | 66.67% | 33.33% | 16.93% | 66.67% | 33.33% | 3.83 | 45.00 |
| BADP Top-3 `.010` | 66.67% | 33.33% | 12.22% | 66.67% | 33.33% | 4.83 | 58.67 |
| BADP Top-5 `.010` | 66.67% | 33.33% | 10.83% | 66.67% | 33.33% | 6.50 | 75.00 |
| BADP Top-5 `.050` | 66.67% | 33.33% | 8.10% | 66.67% | 33.33% | 9.00 | 86.00 |

---

## 5. 결과 해석

### 5.1 BADP의 추가 성공은 관찰되지 않음

3개 질의에서는 Top-3, Top-5 및 모든 BADP 설정이 동일한 66.67%의 정답 도달률을 보였다. BADP가 후보를 더 보존하였지만 추가 정답을 복구하지 못하였다.

특히 BADP Top-3 `.005`는 Top-3보다 평균 폭이 3.00에서 3.83으로 증가했고 확장 후보 수도 43.0에서 45.0으로 증가했지만 정답 도달률은 동일하였다. 이 결과만 보면 해당 질의 집합에서 추가 경계 보존은 불필요한 비용이었다.

### 5.2 넓은 보존이 검색 품질을 낮출 수 있음

BADP Top-5 `.050`은 평균 폭 9.0, 확장 후보 86.0으로 가장 많은 상태를 유지했지만 Macro 검색 F1은 8.10%로 가장 낮았다. 반면 Top-3는 평균 폭 3.0에서 가장 높은 20.63%의 검색 F1을 기록하였다.

이는 앞선 MAGIC 실험과 동일하게 다음 관계를 다시 보여준다.

\[
\text{더 많은 후보 보존}\not\Rightarrow\text{더 높은 유효성}
\]

### 5.3 전역 점수대의 반복적인 불안정성

전역 `.05`와 `.10`은 정답 도달률이 33.33%로 Top-3/Top-5의 절반 수준이었다. 표본이 매우 작아 통계적 결론을 내릴 수는 없지만, WN18RR 실제 반복 탐색에서 전역 additive 방식이 Top-K보다 낮았던 음의 결과와 방향이 일치한다.

### 5.4 BADP의 조건부 작동 필요성

이번 결과는 BADP를 모든 Top-K 단계에서 항상 적용하면 안 된다는 점을 시사한다. BADP의 목적은 탐색 폭을 일반적으로 확대하는 것이 아니라 **Top-K 경계가 실제로 위험한 상황에서만 추가 후보를 보존하는 것**이어야 한다.

따라서 다음 단계에서는 `δ`를 고정하여 매번 추가 후보를 유지하는 방식뿐 아니라, 경계 여유

\[
\Delta_K=s_{(K)}-s_{(K+1)}
\]

가 사전에 정의한 위험 임계값보다 작은 경우에만 BADP를 활성화하는 **조건부 BADP**를 평가할 필요가 있다.

---

## 6. 파일럿의 한계

본 실험은 성능 주장에 사용하기에는 다음 한계가 크다.

1. 질의 수가 3개에 불과하다.
2. ToG와 동일한 Freebase가 아니라 Wikidata를 사용하였다.
3. Wikidata의 outgoing entity-valued relation만 탐색하였다.
4. API 호출 제한 때문에 각 노드 후보를 10개로 사전 축소하였다.
5. 최종 LLM 답안 생성기를 사용하지 않았다.
6. 정답 label 일치 기반의 retrieval 평가이며 전체 WebQSP 공식 평가와 동일하지 않다.

따라서 이 결과를 ToG의 76.2% 또는 다른 peer의 WebQSP 공개 점수와 직접 비교해서는 안 된다.

---

## 7. 후속 실험 설계

본 파일럿 결과를 반영해 다음 실험에서는 다음 세 가지를 변경한다.

### 7.1 그래프 캐시 또는 로컬 그래프 사용

Wikidata API를 질의마다 호출하지 않고 필요한 서브그래프를 사전 수집하여 고정한다. 이를 통해 모든 정책이 완전히 동일한 후보 그래프를 사용하도록 한다.

### 7.2 개발·시험 분리

개발 집합에서 `K`, `δ`, 경계 위험 임계값을 선택한 뒤 시험 집합에서 고정한다.

### 7.3 조건부 BADP

BADP를 모든 단계에 적용하지 않고

\[
\Delta_K\le\tau
\]

인 경우에만 경계 후보를 추가 보존한다. 이를 통해 불필요한 폭 증가를 억제하면서 가지치기 후회가 발생할 가능성이 높은 상황에 집중한다.

최종 비교에서는 Top-K, 전역 점수대, 상대 손실, 항상 활성 BADP, 조건부 BADP를 동일 비용 기준으로 비교한다.
