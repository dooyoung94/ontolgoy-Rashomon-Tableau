# 수집 데이터 기반 누락 토폴로지 관계 복원과 장애 원인 분석

## 초록

운영 환경에서는 서비스, 데이터베이스, 파드, 노드와 같은 객체 자체는 수집할 수 있어도 기존 토폴로지에 객체 사이 관계가 정의되지 않아 연결하지 못하는 경우가 발생한다. 특히 동적 배포, 신규 서비스, 데이터베이스 연결 변경, 외부 호출 변경은 사람이 유지하는 토폴로지와 실제 실행 구조 사이의 차이를 만든다.

본 연구는 **수집 데이터는 존재하지만 기존 토폴로지 관계가 없는 상황**을 대상으로 한다. 완전한 기준 토폴로지에서 관계만 20%, 40%, 60% 제거하고, 추적·지표·로그·자원 정보는 그대로 유지한다. 이후 귀추를 이용해 가능한 관계를 생성하고, DeBERTa 기반 문장 의미 검증과 PSL 기반 규칙 추론으로 관계를 선별한다. 복원 대상은 `CALLS`, `USES_DATABASE`, `DEPLOYED_ON`, `RUNS_ON` 등 운영 구조를 나타내는 관계다.

평가는 두 단계로 구성한다. 첫째, 실제로 제거한 토폴로지 관계를 얼마나 복원했는지 정확도·재현율·F1으로 측정한다. 둘째, 관계가 빠진 토폴로지, 복원한 토폴로지, 완전한 토폴로지를 각각 장애 원인 분석에 사용하여 원인 서비스와 원인 경로 성능이 얼마나 달라지는지 측정한다. 연구의 최종 목적은 관계 복원 자체가 아니라 **복원된 토폴로지가 실제 장애 원인 분석을 개선하는지 검증하는 것**이다.

---

## 1. 문제 정의

### 1.1 현실 문제

수집기는 다음과 같은 객체와 실행 정보를 가져올 수 있다.

```text
Service
Database
Pod
Node
Trace
Metric
Log
```

그러나 기존 토폴로지가 다음처럼 관계 없이 객체만 가지고 있을 수 있다.

```text
payment-api
payment-db
payment-pod-01
node-3
```

실제 실행 데이터에서는 다음 단서가 존재할 수 있다.

```text
payment-api에서 payment-db 접근 흔적
payment-api가 payment-pod-01에서 실행된 흔적
payment-pod-01이 node-3에서 실행된 흔적
frontend 추적의 하위 호출이 order에서 실행된 흔적
```

따라서 필요한 것은 객체를 새로 발견하는 것이 아니라, 이미 수집된 객체 사이에 다음 관계를 복원하는 것이다.

```text
payment-api --USES_DATABASE--> payment-db
payment-api --DEPLOYED_ON----> payment-pod-01
payment-pod-01 --RUNS_ON-----> node-3
frontend --CALLS-------------> order
```

### 1.2 연구 질문

본 연구의 질문은 다음과 같다.

1. 기존 토폴로지 관계가 일부 없을 때 수집 데이터만으로 그 관계를 복원할 수 있는가?
2. 관계 누락 비율이 20%, 40%, 60%로 증가할 때 복원 성능은 어떻게 변하는가?
3. 귀추, 문장 의미 검증, 규칙 추론이 각각 복원 성능에 어떤 영향을 주는가?
4. 복원된 토폴로지를 사용하면 관계가 빠진 토폴로지보다 장애 원인 분석이 개선되는가?

---

## 2. 연구 범위

### 포함

- 서비스 간 호출 관계
- 서비스와 데이터베이스 사용 관계
- 서비스와 파드 배치 관계
- 파드와 노드 실행 관계
- 수집 데이터에서 확인 가능한 메시징 관계

### 제외 또는 제한

- 수집되지 않은 객체 자체를 무제한 생성하는 문제
- 운영자·업무 기능 같은 OpenRCA 데이터에 없는 조직 정보 자동 복원
- 모든 노드 조합을 무작위로 연결하는 방식
- 장애 인과 라벨을 20%, 40%, 60% 제거하는 실험을 주 실험으로 사용하는 방식
- 수집 데이터 자체를 20%, 40%, 60% 삭제하는 방식

---

## 3. 문제의 수식화

완전한 기준 토폴로지를 다음과 같이 둔다.

\[
G=(V,E)
\]

여기서 \(V\)는 수집된 객체 집합이고, \(E\)는 기준 토폴로지 관계 집합이다.

관계 누락 비율을

\[
\rho \in \{0.2, 0.4, 0.6\}
\]

로 두고, 제거할 관계 집합을 \(M_\rho \subset E\)라 한다.

모델이 받는 불완전한 토폴로지는

\[
E_\rho = E \setminus M_\rho
\]

이다.

중요한 점은 수집 데이터 집합 \(O\)는 그대로 유지된다는 것이다.

\[
O_\rho = O
\]

즉 이 실험은 수집 데이터 누락 실험이 아니라 **토폴로지 관계 누락 실험**이다.

모델의 목표는 수집 데이터 \(O\)와 불완전한 토폴로지 \(E_\rho\)를 이용해 누락 관계 \(M_\rho\)를 복원하는 것이다.

\[
\hat{M}_\rho = f(E_\rho, O)
\]

최종 복원 토폴로지는

\[
\hat{E}_\rho = E_\rho \cup \hat{M}_\rho
\]

이다.

주 평가는 \(\hat{M}_\rho\)와 실제 제거한 \(M_\rho\)를 직접 비교한다.

---

## 4. 제안 방법

### 4.1 수집 단서 생성

수집기에서 얻은 원본 데이터에서 객체 사이 연결 가능성을 보여주는 단서를 만든다.

예:

| 수집 단서 | 가능한 관계 |
|---|---|
| 서비스 간 부모-자식 추적 | `CALLS` |
| 서비스와 파드 자원 정보 동시 관측 | `DEPLOYED_ON` |
| 파드와 노드 자원 정보 동시 관측 | `RUNS_ON` |
| 서비스의 데이터베이스 접근 정보 | `USES_DATABASE` |
| 서비스의 메시징 대상 정보 | `USES_MESSAGING` |

코드의 `RelationObservation`은 최종 관계가 아니라 이 **수집 단서**를 저장하는 자료구조다.

### 4.2 귀추 기반 관계 후보 생성

수집 단서가 있는 객체 쌍에 대해서만 가능한 관계를 만든다.

\[
\mathcal{H}(O)=\{(u,r,v)\mid observed(u,v,O) \land allowed(u,r,v)\}
\]

모든 객체 조합을 만들지 않는다. 수집 단서가 전혀 없는 객체 쌍은 임의로 연결하지 않는다.

### 4.3 문장 의미 검증

DeBERTa를 사용하여 수집 내용이 제안된 관계를 의미적으로 지지하는지 확인한다.

예:

```text
수집 내용:
payment-api span에서 PostgreSQL payment-db 접근 관측

관계 후보:
payment-api --USES_DATABASE--> payment-db
```

모델은 후보 관계를 지지하는 정도와 반대하는 정도를 계산한다.

### 4.4 규칙 기반 확률 추론

PSL을 사용하여 귀추 점수와 문장 의미 점수, 관계 종류 제약을 함께 고려한다.

현재 구현에서 PSL의 역할은 관계 후보를 무제한 확장하는 것이 아니라 **이미 수집 단서로 생성된 후보 중 일관된 후보를 선택하는 것**이다.

### 4.5 기존 토폴로지와 병합

기존에 남아 있는 관계는 그대로 유지한다.

```text
기존 관계 + 새로 복원한 관계 = 복원 토폴로지
```

이미 존재하는 관계를 다시 추가한 것은 복원 성능으로 계산하지 않는다.

---

## 5. 비교 방법

| 이름 | 방법 |
|---|---|
| S0 | 관계가 빠진 토폴로지를 그대로 사용 |
| S1 | 귀추로 관계 복원 |
| S2 | 귀추 + DeBERTa |
| S3 | 귀추 + PSL |
| S4 | 귀추 + DeBERTa + PSL |

S0는 복원 방법이 없는 기준선이며, S4가 제안 방법 전체다.

---

## 6. 실험 설계

### 6.1 토폴로지 관계 누락 실험

완전한 기준 토폴로지에서 관계만 다음 비율로 제거한다.

```text
20%
40%
60%
```

각 비율에서 S0~S4를 모두 실행한다.

총 핵심 조건은 15개다.

### 6.2 제거 방식

관계 제거는 다음 조건을 지켜야 한다.

- 객체는 제거하지 않는다.
- 수집 데이터는 제거하지 않는다.
- 제거된 관계를 특수 라벨로 바꾸지 않고 실제로 토폴로지에서 삭제한다.
- 모델은 어떤 관계가 제거되었는지 알 수 없다.
- 동일한 난수 기준에서 20%에 제거된 관계가 40%, 60%에도 포함되도록 중첩시킨다.

### 6.3 주 평가값

주 평가는 실제 제거한 관계만 대상으로 한다.

\[
Precision = \frac{|\hat M \cap M|}{|\hat M|}
\]

\[
Recall = \frac{|\hat M \cap M|}{|M|}
\]

\[
F1 = \frac{2PR}{P+R}
\]

관계 종류별 F1도 계산한다.

보조 평가로 복원 후 전체 토폴로지와 완전한 기준 토폴로지의 F1을 계산한다.

전체 토폴로지 점수를 주 평가로 사용하지 않는 이유는 관계가 20%만 빠졌을 경우 아무것도 복원하지 않아도 80%의 관계가 남아 있기 때문이다.

---

## 7. 장애 원인 분석 평가

토폴로지 관계 복원 효과를 실제 장애 분석까지 연결한다.

각 관계 누락 비율에서 다음 세 조건을 비교한다.

```text
1. 관계가 빠진 토폴로지
2. S4가 복원한 토폴로지
3. 완전한 기준 토폴로지
```

동일한 장애 데이터와 동일한 장애 분석기를 사용한다.

평가값은 다음과 같다.

- 원인 서비스 적중
- 원인 경로 도달 여부
- 노드 F1
- 관계 F1

핵심 비교는 다음이다.

\[
RCA_{incomplete} < RCA_{recovered} \le RCA_{complete}
\]

즉 관계 복원이 실제 장애 원인과 경로 추론을 개선해야 연구의 최종 주장이 성립한다.

---

## 8. OpenRCA 2.0 적용

OpenRCA 2.0은 추적, 지표, 로그와 장애 원인 경로 정답을 제공하므로 장애 분석 평가에 사용할 수 있다.

그러나 OpenRCA 2.0은 `CALLS`, `DEPLOYED_ON`, `RUNS_ON`, `USES_DATABASE` 등을 모두 포함하는 완전한 지속형 운영 토폴로지 정답 파일을 별도로 제공하지 않는다.

따라서 현재 통제 실험에서는 전체 수집 가능 데이터에서 구성한 구조 관계를 **기준 토폴로지**로 두고, 여기서 관계만 일부 제거한다. 이 기준 토폴로지는 실험 평가용이며 제거된 관계는 모델 입력에 제공하지 않는다.

이 실험은 다음과 같이 표현한다.

> **통제된 토폴로지 관계 누락 및 복원 실험**

수동으로 검증된 독립 토폴로지 정답이 확보되면 동일 평가기를 그대로 사용할 수 있다.

---

## 9. 코드와 실험의 대응

| 역할 | 코드 |
|---|---|
| 수집 데이터에서 연결 단서 생성 | `src/openrca_mr/structural.py` |
| 토폴로지 관계 제거 | `src/openrca_mr/topology_recovery.py` |
| 누락 관계 복원 | `src/openrca_mr/topology_recovery.py` |
| 20/40/60% 평가 | `src/openrca_mr/stage1_eval.py` |
| 실행 명령 | `scripts/run_structural_recovery.py` |
| 자동 검증 | `.github/workflows/openrca2-smoke.yml` |

---

## 10. 현재 결과 해석 규칙

이전 개발 과정에서 수행한 다음 결과는 본 논문의 주 결과로 사용하지 않는다.

- 수집 단서를 제거한 실험
- 정상 구간과 장애 구간 구조 일치도 실험
- 장애 인과 라벨 20/40/60% 마스킹 실험

새 문제정의에 맞는 주 결과는 **토폴로지 관계를 20%, 40%, 60% 제거한 뒤 복원한 실험 결과**부터 새로 산출한다.

---

## 11. 기대되는 기여

본 연구의 기여는 특정 모델 조합 자체에만 있지 않다.

1. 수집은 가능하지만 토폴로지 관계가 없는 운영 문제를 명확히 정의한다.
2. 관계가 없는 토폴로지와 실제 수집 단서를 분리하여 평가한다.
3. 제거한 관계만 직접 평가하여 남아 있는 관계로 인한 점수 착시를 막는다.
4. 관계 복원이 실제 장애 원인과 경로 분석을 개선하는지 연결해 검증한다.

---

## 참고 문헌

1. Fang et al., *OpenRCA 2.0: From Outcome Labels to Causal Process Supervision*, 2026.
2. Hobbs et al., *Interpretation as Abduction*, Artificial Intelligence, 1993.
3. He et al., *DeBERTa: Decoding-enhanced BERT with Disentangled Attention*, ICLR, 2021.
4. Bach et al., *Hinge-Loss Markov Random Fields and Probabilistic Soft Logic*, JMLR, 2017.
