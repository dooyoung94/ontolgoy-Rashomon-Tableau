name: OpenRCA2 v1 500 Schema Inspect

on:
  push:
    paths:
      - '.github/workflows/openrca2-v1-inspect.yml'
  workflow_dispatch:

jobs:
  inspect:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - name: Install inspection dependencies
        run: pip install 'huggingface_hub>=0.27,<1' 'pyarrow>=16' 'pandas>=2.1'
      - name: Inspect one public 500-case snapshot case
        shell: bash
        run: |
          python - <<'PY'
          import json
          from pathlib import Path
          from huggingface_hub import list_repo_files, hf_hub_download
          import pyarrow.parquet as pq

          repo = 'lincyaw/openrca2-v1-500'
          files = list_repo_files(repo, repo_type='dataset')
          print('FILE_COUNT', len(files))
          manifest_name = 'MANIFEST.json'
          manifest_path = hf_hub_download(repo, manifest_name, repo_type='dataset')
          manifest = json.loads(Path(manifest_path).read_text())
          rows = manifest.get('cases', manifest if isinstance(manifest, list) else [])
          print('MANIFEST_ROWS', len(rows))
          print('FIRST_MANIFEST_ROW', json.dumps(rows[0], ensure_ascii=False))
          case_name = rows[0]['name']
          case_files = [f for f in files if f.startswith(case_name + '/')]
          print('CASE_NAME', case_name)
          print('CASE_FILES', case_files)
          for filename in case_files:
              if not (filename.endswith('.json') or filename.endswith('.parquet') or filename.endswith('.txt')):
                  continue
              if 'normal_' in filename:
                  continue
              local = hf_hub_download(repo, filename, repo_type='dataset')
              print('\n###', filename)
              if filename.endswith('.json'):
                  obj = json.loads(Path(local).read_text())
                  print(json.dumps(obj, ensure_ascii=False)[:12000])
              elif filename.endswith('.txt'):
                  print(Path(local).read_text()[:4000])
              else:
                  pf = pq.ParquetFile(local)
                  print('SCHEMA', pf.schema_arrow)
                  print('N_ROWS', pf.metadata.num_rows)
                  table = pf.read().slice(0, 3)
                  print('ROWS', table.to_pylist())
          PY
